import { ImageFormat } from '../types/Image';
import { SearchResultAlbum, SearchResultAny, SearchResultArtist, SearchResultFan, SearchResultLabel, SearchResultTrack, SearchResults } from '../types/Search';
export declare enum SearchItemType {
    All = "All",
    ArtistsAndLabels = "ArtistsAndLabels",
    Albums = "Albums",
    Tracks = "Tracks",
    Fans = "Fans"
}
export interface SearchAPISearchParams {
    query: string;
    page?: number;
    albumImageFormat?: string | number | ImageFormat;
    artistImageFormat?: string | number | ImageFormat;
}
export default class SearchAPI {
    static all(params: SearchAPISearchParams): Promise<SearchResults<SearchResultAny>>;
    static artistsAndLabels(params: SearchAPISearchParams): Promise<SearchResults<SearchResultArtist | SearchResultLabel>>;
    static albums(params: SearchAPISearchParams): Promise<SearchResults<SearchResultAlbum>>;
    static tracks(params: SearchAPISearchParams): Promise<SearchResults<SearchResultTrack>>;
    static fans(params: SearchAPISearchParams): Promise<SearchResults<SearchResultFan>>;
    /**
     * @internal
     */
    protected static search(params: SearchAPISearchParams & {
        itemType: SearchItemType.ArtistsAndLabels;
    }): Promise<SearchResults<SearchResultArtist | SearchResultLabel>>;
    protected static search(params: SearchAPISearchParams & {
        itemType: SearchItemType.Albums;
    }): Promise<SearchResults<SearchResultAlbum>>;
    protected static search(params: SearchAPISearchParams & {
        itemType: SearchItemType.Tracks;
    }): Promise<SearchResults<SearchResultTrack>>;
    protected static search(params: SearchAPISearchParams & {
        itemType: SearchItemType.Fans;
    }): Promise<SearchResults<SearchResultFan>>;
    protected static search(params: SearchAPISearchParams & {
        itemType: SearchItemType.All;
    }): Promise<SearchResults<SearchResultAny>>;
    /**
     * @internal
     */
    protected static getSearchUrl(params: SearchAPISearchParams & {
        itemType: SearchItemType;
    }): string;
}
export declare class LimiterSearchAPI extends SearchAPI {
    static all(params: SearchAPISearchParams): Promise<SearchResults<SearchResultAny>>;
    static artistsAndLabels(params: SearchAPISearchParams): Promise<SearchResults<SearchResultArtist | SearchResultLabel>>;
    static albums(params: SearchAPISearchParams): Promise<SearchResults<SearchResultAlbum>>;
    static tracks(params: SearchAPISearchParams): Promise<SearchResults<SearchResultTrack>>;
    static fans(params: SearchAPISearchParams): Promise<SearchResults<SearchResultFan>>;
}
//# sourceMappingURL=SearchAPI.d.ts.map